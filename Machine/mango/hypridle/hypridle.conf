general {
    lock_cmd = pidof hyprlock || hyprlock
    ignore_dbus_inhibit = false
}
listener {
    type = playerctl
}
listener {
    timeout = 300
    on-timeout = hyprlock
}

listener {
    timeout = 600
    on-timeout = systemctl suspend
}   